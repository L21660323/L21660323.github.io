// Esperamos a que la página esté completamente cargada
window.onload = function() {
    // Seleccionamos el elemento con el id 'soyelid'
    const elemento = document.getElementById('soyelid');
    
    // Cambiamos el contenido del elemento
    elemento.textContent = '¡No me gusta la programación!';
};